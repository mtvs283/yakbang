# 약방광개토

Standalone Next.js MVP for the Yakbang Gwanggaeto royal-remedy hotspot screen.

## Local Dev

Save the illustration as `public/yakbang-bg.png`, then run:

```bash
npm install
npm run dev
```

In development, use the bottom-left `hotspot guide` toggle to show the clickable area.
